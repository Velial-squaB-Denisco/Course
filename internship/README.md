### INTERNSHIP 

# 1. Сервис FastAPI (синхронный/асинхронный)

Давай представим, что ты хочешь сделать приложение, как WhatsApp или Instagram. Твоё приложение должно уметь:
1. Принимать сообщения от пользователей
2. Обрабатывать их
3. Отправлять ответы

**FastAPI** — это как волшебный конструктор для создания таких "общающихся" программ. Представь, что это набор кубиков Лего, где каждый кубик отвечает за разную часть твоего приложения.

---

### 🌟 Простыми словами, что это:
Это инструмент на Python, который помогает быстро создавать "мосты" между:
- Твоей программой на компьютере
- Телефоном/компьютером пользователя
- Другими программами

Пример: когда ты пишешь сообщение в мессенджере, FastAPI как почтальон:
1. Берёт твоё сообщение
2. Относит его нужному человеку
3. Приносит ответ обратно

---

### 🛠️ Как это работает (на примере пиццерии):
1. **Меню (API Endpoints)**:
   - Ты создаёшь "меню" из возможных действий:
     - `/order-pizza` — заказать пиццу
     - `/check-status` — проверить статус заказа

2. **Повар (Ваш код на Python)**:
   - На каждый пункт меню пишешь инструкцию:
   ```python
   from fastapi import FastAPI
   app = FastAPI()

   @app.get("/order-pizza")
   def make_pizza():
       return {"status": "Готовится!"}
   ```

    Здесь мы определяем маршрут (endpoint) для обработки GET-запросов по адресу /order-pizza.

    Декоратор @app.get("/order-pizza"): Этот декоратор указывает, что функция make_pizza будет вызвана, когда клиент отправит GET-запрос по адресу /order-pizza.

    Функция make_pizza: Эта функция обрабатывает запрос и возвращает JSON-ответ. В данном случае, она возвращает объект с ключом "status" и значением "Готовится!".

3. **Клиент (Пользователь)**:
   - Заходит по адресу `ваш-сайт.ru/order-pizza`
   - Видит ответ: `{"status": "Готовится!"}`

---

### 🔥 Главные фишки:
1. **Скорость**:
   - Работает как гоночный болид (один из самых быстрых фреймворков)
   - Обрабатывает тысячи запросов в секунду

2. **Автодокументация**:
   - Сам создаёт инструкцию к вашему API
   - Просто открой `/docs` и увидишь всё как в Swagger

3. **Проверка данных**:
   - Если пользователь прислал ерунду (например, буквы вместо цифр), FastAPI сам скажет об ошибке:
   ```python
   from pydantic import BaseModel

   class Order(BaseModel):
       pizza_type: str
       quantity: int

   @app.post("/order")
   def create_order(order: Order):
       # Если quantity не число — ошибка появится автоматически
       return {"message": f"Готовим {order.quantity} пицц!"}
   ```

---

### 🧩 Для чего используют:
1. **Мобильные приложения** — чтобы телефон мог общаться с сервером
2. **Веб-сайты** — для динамического контента
3. **Микросервисы** — когда большая система разбита на маленькие части
4. **Машинное обучение** — чтобы дать доступ к вашей AI-модели через API

---

### 🚀 Пример "Hello World":
1. Установка:
   ```bash
   pip install fastapi uvicorn
   ```

2. Код (`main.py`):
   ```python
   from fastapi import FastAPI

   app = FastAPI()

   @app.get("/hello")
   def say_hello():
       return {"message": "Привет, друг!"}
   ```

3. Запуск:
   ```bash
   uvicorn main:app --reload
   ```

4. Проверь:
   - Открой `http://localhost:8000/hello` → увидишь приветствие
   - Зайди на `http://localhost:8000/docs` → увидишь автоматическую документацию

---

### 💡 Почему новичкам легко:
- Пишешь обычный Python-код
- Не нужно разбираться в сложных настройках
- Ошибки показываются понятно (как учитель, который подсказывает где ошибся)
- Можно постепенно учить — начать с простого, добавлять сложное

---

### 🆚 Сравнение с другими:
|               | FastAPI | Flask | Django |
|---------------|---------|-------|--------|
| Скорость       | 🚀       | 🐢     | 🚗      |
| Сложность      | 😊       | 😊     | 😓      |
| Документация   | Авто    | Ручная| Ручная |
| Поддержка async| Да      | Нет   | Да     |

---

**Итог:** FastAPI — это как супер-помощник для создания современных веб-приложений. Даже если ты никогда не делал API, за вечер сможешь сделать работающий прототип! Он берёт на себя всю рутину, а ты фокусируешься на логике приложения.

# 2. SQLalchemy

Давай представим, что ты хочешь управлять своей коллекцией игрушек, но вместо коробки у тебя есть умная база данных. SQLAlchemy — это как волшебный робот-помощник, который делает всю грязную работу за тебя!

### 1. **Что такое SQLAlchemy?**
Это библиотека Python, которая помогает общаться с базами данных (как Excel для данных, но мощнее). Вместо того чтобы писать сложные команды на языке SQL (который как иностранный язык для баз данных), ты пишешь обычный Python-код, а SQLAlchemy переводит его в SQL.

---

### 2. **Зачем это нужно?**
- **Не хочешь учить SQL?** SQLAlchemy позволяет работать с данными как с объектами Python.
- **Безопасность:** Автоматически защищает от хакерских атак (например, SQL-инъекций).
- **Гибкость:** Работает с разными базами (SQLite, MySQL, PostgreSQL) без изменения кода.
- **Удобство:** Не нужно вручную писать сотни строк SQL.

---

### 3. **Как это работает? Две основные "суперсилы":**
#### a) **SQLAlchemy Core** — "Механическая рука"
- Низкоуровневый подход. Ты пишешь команды, похожие на SQL, но на Python.
- Пример: 
  ```python
  from sqlalchemy import create_engine, Table, Column, Integer, String

  # Подключаемся к базе (например, SQLite)
  engine = create_engine('sqlite:///toys.db')

  # Описываем таблицу "Игрушки"
  toys_table = Table('toys', metadata,
      Column('id', Integer, primary_key=True),
      Column('name', String),
      Column('color', String)
  )

  # Создаем таблицу
  metadata.create_all(engine)

  # Добавляем игрушку
  with engine.connect() as connection:
      connection.execute(toys_table.insert(), {"name": "Робот", "color": "синий"})
  ```

  ### 1. **Создание таблицы «toys»**  
```python
toys_table = Table('toys', metadata, ...)
```
- **`'toys'`** — название таблицы в базе данных. Это как имя файла (например, `игрушки.xlsx`).
- **`metadata`** — это «папка», где хранятся все схемы таблиц. Если ты создаешь несколько таблиц, `metadata` запоминает их все, чтобы потом можно было разом создать в базе.

---

### 2. **Колонки — это столбцы таблицы**  
Каждая колонка описывает, какие данные будут храниться.

#### a) Колонка `id`:
```python
Column('id', Integer, primary_key=True)
```
- **`'id'`** — название колонки.
- **`Integer`** — тип данных: целые числа (1, 2, 3...).
- **`primary_key=True`** — это главный идентификатор. Каждая игрушка получает уникальный номер (как серийный номер на телефоне).  
**Зачем?** Чтобы можно было точно найти игрушку, даже если есть дубликаты имен.

#### b) Колонка `name`:
```python
Column('name', String)
```
- **`String`** — тип данных: текст (например, "Робот", "Машинка").

#### c) Колонка `color`:
```python
Column('color', String)
```
- Аналогично: хранит цвет игрушки текстом ("красный", "синий").

---

### 3. **Как это выглядит в базе?**  
После создания таблица `toys` будет такой:

| id (Integer) | name (String) | color (String) |
|--------------|---------------|----------------|
| 1            | Робот         | синий          |
| 2            | Машинка       | красный        |

- Каждая новая запись (строка) автоматически получает уникальный `id`.
- Если попытаться записать в `color` число (например, 123), SQLAlchemy выдаст ошибку — потому что мы указали тип `String`.

---

### 4. **Что делает `metadata`?**  
- Это как **«чертеж»** всей базы данных. Когда ты добавляешь таблицу в `metadata`, SQLAlchemy запоминает ее структуру.
- Чтобы создать таблицу в реальной базе, нужно выполнить:
  ```python
  metadata.create_all(engine)
  ```
  - `engine` — это подключение к базе (например, к файлу `toys.db`).
  - Эта команда превращает «чертеж» в реальную таблицу.

---

### 5. **Пример с ошибкой**  
Допустим, ты забыл добавить колонку `size`:
```python
# Пропустили колонку:
toys_table = Table('toys', metadata,
    Column('id', Integer, primary_key=True),
    Column('name', String)
)
```
- Если потом попытаешься записать в таблицу цвет (`color`), возникнет ошибка — колонки `color` нет в схеме!

---

#### b) **SQLAlchemy ORM** — "Волшебный переводчик"
- Высокоуровневый подход. Ты работаешь с данными как с объектами Python.
- Пример:
  ```python
  from sqlalchemy.orm import declarative_base, sessionmaker

  Base = declarative_base()

  # Создаем класс "Игрушка" (это как шаблон для строк в таблице)
  class Toy(Base):
      __tablename__ = 'toys'
      id = Column(Integer, primary_key=True)
      name = Column(String)
      color = Column(String)

  # Подключаемся к базе
  engine = create_engine('sqlite:///toys.db')
  Session = sessionmaker(bind=engine)
  session = Session()

  # Добавляем новую игрушку
  new_toy = Toy(name="Машинка", color="красная")
  session.add(new_toy)
  session.commit()  # Сохраняем в базе

  # Ищем все красные игрушки
  red_toys = session.query(Toy).filter_by(color="красная").all()
  ```
  
---

### 6. **Зачем всё это?**  
- **Контроль над данными:** Ты заранее говоришь базе, какие данные можно хранить (как правила игры).
- **Безопасность:** SQLAlchemy сам проверяет типы данных. Не даст записать текст туда, где должно быть число.
- **Удобство:** Если захочешь добавить новую колонку (например, `price`), достаточно изменить схему и вызвать `metadata.create_all(engine)` заново.

---

### 4. **Основные понятия простыми словами:**
- **Движок (Engine):** Как шнур питания. Отвечает за подключение к базе.
- **Сессия (Session):** Как блокнот, где временно записываются изменения. Когда готово — сохраняешь (`commit()`).
- **Модель (Model):** Класс Python, который описывает таблицу в базе (например, класс `Toy`).
- **Метаданные (Metadata):** Каталог всех таблиц, которые ты создал.

---

### 5. **Пример из жизни:**
Представь, что у тебя есть таблица в Excel с игрушками:
| id  | name      | color    |
|-----|-----------|----------|
| 1   | Робот     | синий    |
| 2   | Машинка   | красная  |

С SQLAlchemy ORM:
- Каждая строка — это объект `Toy`.
- Чтобы добавить новую игрушку, ты просто создаешь объект `Toy(name="Кукла", color="розовая")` и говоришь сессии: «Запомни это!».
- Когда вызываешь `session.commit()`, робот-помощник сам вставляет новую строку в Excel (базу данных).

  ```python

  # Импортируем необходимые компоненты
   from sqlalchemy import create_engine, Column, Integer, String
   from sqlalchemy.orm import declarative_base, sessionmaker
   
   # 1. Создаем "базу" для наших моделей (как чертеж)
   Base = declarative_base()
   
   # 2. Описываем класс Игрушка (это наша таблица)
   class Toy(Base):
       __tablename__ = 'toys'
       id = Column(Integer, primary_key=True)  # Уникальный ID
       name = Column(String)                   # Название игрушки
       color = Column(String)                  # Цвет
   
   # 3. Подключаемся к базе данных (создаем файл toys.db)
   engine = create_engine('sqlite:///toys.db')
   
   # 4. Создаем все таблицы (если их нет)
   Base.metadata.create_all(engine)
   
   # 5. Создаем "сессию" — это как блокнот для временных записей
   Session = sessionmaker(bind=engine)
   session = Session()
   
   # 6. Создаем новую игрушку-объект
   new_toy = Toy(name="Кукла", color="розовая")
   
   # 7. Говорим сессии: "Запомни эту игрушку!"
   session.add(new_toy)
   
   # 8. Сохраняем изменения в базе (как Ctrl + S в Excel)
   session.commit()
   
   # 9. Закрываем блокнот (сессию)
   session.close()

  ```

---

### 6. **Плюсы SQLAlchemy:**
- **Код на Python, а не SQL:** Пишешь на знакомом языке.
- **Автоматизация:** Создание таблиц, проверка типов данных.
- **Безопасность:** Нет риска сломать запрос из-за опечатки в SQL.
- **Переносимость:** Переключиться с SQLite на PostgreSQL можно одной строкой кода.

---

### 7. **Когда использовать?**
- Если твое приложение работает с данными (пользователи, заказы, посты).
- Если хочешь избежать ручного написания SQL.
- Если планируете масштабировать проект (например, сменить базу данных).

---

**Итог:** SQLAlchemy — это мост между твоим Python-кодом и базой данных. Ты работаешь с данными как с объектами, а библиотека заботится о сложных SQL-запросах. Это как если бы у тебя был личный переводчик, который говорит и на Python, и на SQL!
